{
  "name": "@downtrace/bench",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic load generator and agent overhead benchmark for the reference app",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "load": "node src/load-cli.ts",
    "bench": "node src/bench-cli.ts",
    "test": "vitest run",
    "test:integration": "vitest run integration",
    "typecheck": "tsc -p tsconfig.json",
    "instruments": "node src/instruments-cli.ts"
  },
  "devDependencies": {
    "@downtrace/reference-app": "workspace:*"
  }
}
